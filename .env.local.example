# FastAPI backend base URL (no trailing /api)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Google OAuth Web client ID (same one configured as GOOGLE_CLIENT_ID on the backend)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
